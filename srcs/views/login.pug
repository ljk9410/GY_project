extends base

block content
    div.login__form-container
        h2 경제연구회 관리자 로그인
        if errorMessage
            span=errorMessage 
        form(method="POST")
            input(type="text" name="id" placeholder="아이디")
            input(type="password" name="password" placeholder="비밀번호")
            input(type="submit" value="로그인")